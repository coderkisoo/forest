<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/base_background"
    android:gravity="center_horizontal"
    android:orientation="vertical">

    <android.support.v7.widget.Toolbar
        android:id="@+id/tl_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:navigationIcon="@mipmap/back">


    </android.support.v7.widget.Toolbar>


    <TextView
        android:id="@+id/tv_percent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:padding="10dp"
        android:textColor="#ffffff"
        android:textSize="30sp"
        android:textStyle="italic"
        tools:text="成功时长占比：24%" />

    <cn.kisoo.forest.view.CircleWidget
        android:id="@+id/cw_widget"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_marginTop="20dp"
        app:bottom_color="#6ADBE8"
        app:min_top_color="#FF939C"
        app:pic_padding="-1dp"
        app:ring_radii="130dp"
        app:ring_width="13dp"
        app:top_color="#006E7A"
        app:touchable="false" />

    <TextView
        android:id="@+id/tv_direction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:padding="10dp"
        android:text="@string/bird_died"
        android:textColor="#ffffff"
        android:textSize="24sp"
        tools:text="@string/bird_died" />

    <Button
        android:id="@+id/btn_do_next"
        android:layout_width="120dp"
        android:layout_height="40dp"
        android:layout_marginTop="50dp"
        android:background="@drawable/round_button"
        android:text="@string/do_next"
        android:textColor="#FFFFFF"
        android:textSize="18sp" />

</LinearLayout>
